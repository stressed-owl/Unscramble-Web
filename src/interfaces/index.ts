export interface ClassName {
    className?: string;
}

export interface Children {
    children: React.ReactNode;
}

export interface Word {
    id: number;
    word: string;
    definition: string;
    language: string;
}